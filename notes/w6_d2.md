# W6 Day 2 — MC Option Pricer + Header Architecture

**Date:** 2026-05-13
**Session type:** Day 2 (solo implementation)

---

## What we built

`MCPricer` class in `mc_pricer.h` / `mc_pricer.cpp`:
- Takes S0, K, r, sigma, T, N
- Runs N GBM paths via `GBMSimulator`
- Computes `max(S_T - K, 0)` per path, averages, discounts
- At N=100,000: 10.4384 vs BS closed form 10.45 — within convergence band ✓

Header architecture cleaned up across all three Week 6 classes:
- `normal_sampler.h` — header guards, `<random>` only
- `gbm_simulator.h` — header guards, `<cmath>`, `<vector>`, `"normal_sampler.h"`
- `mc_pricer.h` — header guards, `<algorithm>`, `<vector>`, `<cmath>`, `"gbm_simulator.h"`

---

## Retrieval scores

| Topic | Score | Notes |
|-------|-------|-------|
| GBM formula from memory | Full credit | Structure, drift/shock split, notation (`rt` ambiguity flagged and corrected) |
| σ²/2 mechanism | Partial → full | Named "log-normal asymmetry correction" correctly; needed numerical hint (e^0.1 + e^-0.1)/2 = 1.005 to articulate full mechanism; then nailed it in one sentence |
| Call payoff formula `max(S_T - K, 0)` | Unaided | Retrieved correctly when prompted |

---

## Key wins

- **[FIRST TIME]** `#include` mechanics understood as preprocessor copy-paste, not a function call — derived from `#include <iostream>` pattern
- **[FIRST TIME]** Header guards written correctly and independently on all three headers
- **[FIRST TIME]** Header/source split applied across a multi-file project
- σ²/2 full mechanism articulated cleanly after one hint: "e^x is convex — upside lifts more than downside drops; σ²/2 subtracts that excess so expected price grows at exactly r"

## Bugs caught

1. `const& x` in range-for missing type → `const double& x`
2. `sim_vec[251]` wrong index (second-to-last not last) → `sim_vec.back()`
3. Loop hardcoded `252` instead of `_N`
4. `#include <iostream>` in headers where unused
5. Missing `#include <algorithm>` for `std::max`
6. `normal_sampler.cpp` duplicated class definition instead of being a driver

---

## Carry-forwards into W6 Day 3

- `push_back` vs pre-initialized increment — re-test due 2026-05-14
- Three roles of `*` — re-test due 2026-05-15
- T& vs T* distinctions — re-test due 2026-05-15
- MC convergence rate O(1/√N) — not yet explicitly tested; introduce Day 3 or Day 4

---

## Full session log (closing sequence)

**Session header**
- Date: 2026-05-13 | Week 6, Day 2 | Role: Learning Coach
- Attempted: retrieval re-tests (GBM formula, σ²/2) + solo MCPricer implementation + header architecture cleanup
- Completed: all of the above — W6 acceptance criterion green

---

**Exercises worked**

*Exercise 1 — GBM formula retrieval (opening re-test)*
- Objective: write formula from memory, identify deterministic vs random parts, explain σ²/2
- Solo attempt: yes — user wrote full formula unprompted
- Hints given: one precision correction ("`rt`" notation ambiguous — should be `r`, not `r*t`; units of time-squared otherwise)
- Acceptance: passed — structure, split, and naming all correct
- Quality note: "log-normal asymmetry correction" named correctly but mechanism not yet articulated; pushed to separate exercise below

*Exercise 2 — σ²/2 mechanism (Jensen's inequality)*
- Objective: articulate WHY σ²/2 appears in the GBM drift, not just name it
- Solo attempt: user said "log-normal asymmetry correction" — correct label, no mechanism
- Hint 1 (numerical): "Compute (e^0.1 + e^-0.1) / 2. Why this direction: the number will tell you whether symmetric log returns produce a price bias, and which direction." User got 1.005.
- Acceptance: passed — after the numerical hint, user articulated: "e^x is convex — upside lifts more than downside drops; σ²/2 subtracts that excess so expected price grows at exactly r." One precision upgrade applied: asymmetry is in the exponential function, not in log returns themselves.
- Score: 1/2 (one hint)

*Exercise 3 — MCPricer solo implementation*
- Objective: implement MC European call pricer using GBMSimulator; converge to BS closed form
- Solo attempt: yes — user wrote pseudo-code first, then real implementation
- Hints given:
  - "Code belongs inside a method, not floating in the class body. Why: class bodies declare members; only method bodies execute." 
  - "GBMSimulator as a member requires a default constructor — it doesn't have one. Why: creating it inside price_call() is simpler; member storage requires constructor args you haven't parameterised."
  - "You're averaging S_T not the payoff. Why this direction: option price is E[payoff] discounted, not E[S_T] discounted — very different numbers."
- Acceptance: green — N=100,000 produces 10.4384 vs BS 10.45; within convergence band ✓
- Bugs caught (code review): wrong index `sim_vec[251]` (should be `back()`); `_N` unused in loop; `const& x` missing type; duplicate class definition in `.cpp`; unused `<iostream>` in headers; missing `<algorithm>` for `std::max`

*Exercise 4 — Header architecture*
- Objective: understand `#include` mechanics, apply header guards, fix include sets
- Triggered by: user asked "can I use a class from another file?" — organic entry point
- Hints given: none on `#include` mechanics — user derived preprocessor copy-paste from `#include <iostream>` pattern unaided [FIRST TIME]
- Header guards: explained once, user applied to all three files correctly on first attempt [FIRST TIME]
- Include hygiene: `<iostream>` removed from headers that didn't use it; `<vector>` and `<cmath>` added to `gbm_simulator.h`; `<algorithm>` added to `mc_pricer.h`

---

**Conceptual wins**

- σ²/2 as Jensen's inequality: "because e^x is convex, symmetric log returns produce average prices above 1.0 — σ²/2 subtracts the excess." One hint needed. [NOT FIRST TIME — carried from W4, but first clean one-sentence articulation]
- `#include` as preprocessor copy-paste: derived independently from `#include <iostream>` pattern without prompting. [FIRST TIME]
- Header guards: `#ifndef / #define / #endif` pattern understood and applied correctly to all three files independently. [FIRST TIME]
- `max(S_T - K, 0)` payoff retrieved correctly when asked; confirmed understanding that MC averages payoffs, not prices.

---

**Scaffolding log**

1. User said "samples from the log-normal distribution" for the shock term Z. Correction: Z is standard normal N(0,1); *prices* are log-normally distributed. Landed on first statement.
2. User thought `#include` "calls pre-initialized class definitions." Correction: it is a literal preprocessor copy-paste — the compiler never sees the directive, only the pasted text. Landed immediately after explanation.
3. User wanted to store `GBMSimulator` as a class member. Issue: no default constructor; also confused N (path count) with steps (time steps per path). Resolution: create locally in `price_call()`, explained the distinction. Landed.
4. User tried to average S_T values rather than payoffs. Coaching question ("what do you compute from S_T before averaging?") resolved it without giving the answer.

---

**Retrieval scores (0 = scaffolded, 1 = one hint, 2 = unaided)**

| Prompt | Score | Notes |
|--------|-------|-------|
| GBM formula from memory | 2 | Structure and split unaided; `rt` notation corrected |
| σ²/2 mechanism (full, one sentence) | 1 | Named correctly; numerical hint needed for mechanism |
| Call payoff `max(S_T - K, 0)` | 2 | Unaided when prompted |
| `#include` mechanics | 2 | Derived from first principles [FIRST TIME] |

---

**Momentum and blockers**

- Pace: on track. W6 Day 2 complete; acceptance criterion green same session.
- Next-session risk: `push_back` vs pre-initialized increment (due 2026-05-14) — scored 1/2 at last test, still sticky. Open with it.
- One thing to re-test first: "What are the three roles of `*` in C++?" — due 2026-05-15, haven't tested this session.

---

**Next session's contract**

- **First action:** W6 Day 3 — open with "What are the three roles of `*` in C++?" before any new content.
- **Retrieval question:** "Without looking: what are the three roles of `*` in C++? Give one example of each."
- **Carry-forwards:** `push_back` vs pre-initialized increment (due 2026-05-14), three roles of `*` (due 2026-05-15), T& vs T* (due 2026-05-15), MC convergence O(1/√N) not yet tested.
