# W8 Day 2 — 2026-05-22

## Session header

- **Date:** 2026-05-22
- **Week / Day:** Week 8, Day 2
- **Roles active:** Learning Coach
- **Duration:** ~2h
- **Attempted:** Full multi-file VaR (Value at Risk — loss threshold exceeded only X% of the time) implementation: csv_loader.h, csv_loader.cpp, normal_sampler.h (header-only from W6), var_model.h, var_model.cpp.
- **Completed:** All five files implemented. Compiled clean, zero warnings under `-Wall -Wextra -std=c++20`. Program ran with sensible output — three methods agree within ~0.001; 99% VaR more negative than 95% VaR for all three methods.

---

## Exercises worked

### 1. Session warm-up — GBM formula (spaced retrieval from W6, overdue 2026-05-20)

- **Objective:** Write the GBM (Geometric Brownian Motion — a model for how prices evolve randomly over time) path update formula from memory: both the drift term and the shock term.
- **Solo attempt:** Yes.
- **Hints given:** One algebraic coaching question after notation error — "expand (sigma/2)^2 and compare to sigma^2/2" (direction: algebraic expansion reveals the two are unequal, pointing at the specific wrong term).
- **Result:** Self-corrected to `S_next = S_current * e^((r - σ²/2)*dt + σ*√dt*Z)`. Score: 1/2.
- **Note:** Structure and shock term were correct; error was notation — wrote (σ/2)² (= σ²/4) not σ²/2.

---

### 2. Session warm-up — VaR index arithmetic (retested from W8D1)

- **Objective:** For 500 log returns sorted ascending, give the single index for 95% VaR and for 99% VaR.
- **Solo attempt:** Yes.
- **Result:** 2/2 unaided — index 24 for 95%, index 4 for 99%. Both immediate. Improvement from W8D1 where 95% needed a coaching step.

---

### 3. Architecture design

- **Objective:** Decide the file structure for the W8 project before writing code.
- **Solo attempt:** Yes — user proposed header files for CSV, NormalSampler, and "Monte Carlo simulator" (terminology corrected to Monte Carlo).
- **Hints given:** One coaching question — "what state would a CSVLoader class hold between calls?" (direction: state is the reason to use a class; no state = no class). User arrived at "nothing" and decided free function.
- **Result:** Correct 5-file structure agreed: csv_loader.h, csv_loader.cpp, normal_sampler.h (header-only), var_model.h, var_model.cpp.
- **Key design reasoning landed:** No state = no class. Reusing W6/W7 code = composition, not rewrite.

---

### 4. csv_loader.h — declarations only

- **Objective:** Move W7 function declarations to a header with guards, minimal includes.
- **Solo attempt:** Yes.
- **Bugs caught:**
  - Full function definitions in the header instead of declarations. Coaching: "what's actually in here?" → user identified the issue and moved bodies to .cpp.
  - Missing `<utility>` for `std::pair` (caught later, added).
- **Result:** Clean header: `#ifndef` guard, `<string>` and `<vector>` includes only, five declarations with semicolons.

---

### 5. csv_loader.cpp — definitions + new load_prices()

- **Objective:** Extract CSV-reading logic from W7's `main()` into `load_prices(filepath)`, fix includes, remove noisy row-by-row print.
- **Solo attempt:** Yes.
- **Bugs caught:**
  - `#include <ifstream>` doesn't exist → corrected to `<fstream>`. Coaching: "one of these headers doesn't exist as a standard header — which one?"
  - Missing `<iostream>` (needed for `std::cerr`) and `<cmath>` (needed for `std::log`, `std::sqrt`).
  - `load_prices()` ignored its `filepath` parameter and used hardcoded `"data/prices.csv"` → fixed to use parameter.
  - `std::cout << date << " -> " << price` inside a utility function — noisy in W8 context where caller doesn't want per-row output. Removed.
- **Result:** Correct. Five functions defined, `load_prices()` uses `filepath`, no print inside utility.

---

### 6. var_model.h — three VaR function signatures

- **Objective:** Declare `historical_VaR`, `parametric_VaR`, `monte_carlo_VaR` with correct return type and parameters.
- **Solo attempt:** Yes.
- **Bugs / design errors caught:**
  - Return type `void` for all three. Coaching: "what would a caller want to do with the result?" → user reached "further processing" → `std::pair<double,double>` for 95% and 99%.
  - `parametric_VaR` had a `seed` parameter (parametric VaR has no randomness) and both `price_vec` AND `mean`/`stddev` (redundant — if you have the data, compute inside; if you pass stats, don't pass data). Coached away redundancy; user settled on `(double mean, double stddev)`.
  - `historical_VaR` parameter named `price_vec` — historical VaR sorts log returns, not prices. Renamed to `log_returns`.
  - `monte_carlo_VaR` had GBM parameters (`S0, r, sigma, T, steps`) — user confused MC VaR with the W6 GBM path simulator. Coaching: "go back to what W8D1 said MC VaR does step by step." User recalled: fit μ/σ from data, draw N samples from N(μ,σ), sort, index. Parameters corrected to `(double mean, double sigma, int N, unsigned int seed)`.
  - Missing `#include <utility>` for `std::pair`.
- **Result:** Correct. Three signatures returning `std::pair<double,double>` with correct parameters.

---

### 7. var_model.cpp — historical_VaR

- **Objective:** Sort a copy of log returns, index at 5% and 1%.
- **Solo attempt:** Yes.
- **Bugs caught:**
  - `std::sort` on a `const` reference — compile error. Coaching: "will std::sort compile on a const reference? what does const promise?" → user reached "non-mutable" → made local copy first.
  - After making the copy, indexed into the original unsorted vector (`log_returns`) instead of the sorted copy (`log_return_cpy`). Coaching: "which vector should you read from?"
  - Index arithmetic: `size * 5/100` evaluates left-to-right as `(size * 5)/100 = 25` for n=500 — off by one. User had already derived index 24 in warm-up, so accepted the `-1` correction quickly.
- **Result:** Correct. Makes sorted copy, indexes into it at `(size*5/100)-1` and `(size*1/100)-1`.

---

### 8. var_model.cpp — parametric_VaR

- **Objective:** Apply μ + z×σ with z = −1.645 (95%) and z = −2.326 (99%).
- **Solo attempt:** Yes.
- **Bug caught:** z-score for 99% written as −2.236 (digit transposition). User had stated −2.326 correctly two messages earlier. Coaching: "you stated it correctly just now — what is the 99% z-score?" Self-corrected immediately.
- **Result:** Correct. Two-line function.

---

### 9. var_model.cpp — monte_carlo_VaR

- **Objective:** Construct NormalSampler, draw N returns, sort, index at percentiles.
- **Solo attempt:** Yes.
- **Bug caught:** Loop condition `i < N + 1` draws N+1 samples instead of N. Coaching: "how many times does this loop iterate?" Self-corrected to `i < N`.
- **Result:** Correct.

---

### 10. var_model.cpp — main()

- **Objective:** Orchestrate the pipeline: load → log returns → stats → three VaR calls → print.
- **Solo attempt:** Yes.
- **Bugs caught:**
  - Missing `#include "csv_loader.h"` and `#include "normal_sampler.h"` — functions invisible to compiler.
  - N = 10,000 instead of 100,000 (curriculum specifies 100k draws).
  - `std::cout << "label: ", var95 << ...` — comma operator instead of `<<`. Coaching: "one operator is wrong — look at where you used commas instead."
- **Result:** Correct. Structured bindings (`auto [var95, var99] = pair`) used correctly for output.

---

### 11. Compilation and run

- **First attempt:** 1 compile error (expected semicolon from an earlier draft state). Fixed.
- **Second attempt:** Clean compile, zero warnings under `-Wall -Wextra -std=c++20`.
- **Output:**
  ```
  malformed row: BADROW   (×3 — CSV guard working)
  Historical:  95% VaR = −0.0256 || 99% VaR = −0.0357
  Parametric:  95% VaR = −0.0254 || 99% VaR = −0.0359
  Monte Carlo: 95% VaR = −0.0254 || 99% VaR = −0.0359
  ```
- **Sanity checks:** 99% VaR more negative than 95% for all three ✓. Three methods agree within ~0.001 ✓.

---

## Conceptual wins

- **No state = no class** — arrived after one coaching question (what state would a CSVLoader hold?). Unaided conclusion. No [FIRST TIME] — they've known this since W6.
- **Definitions in headers cause problems** — after coaching, correctly moved all bodies to .cpp. Understood the rule even if they needed a prompt to apply it.
- **Utility functions should not print** — accepted reasoning without resistance: "caller doesn't want per-row noise."
- **Fat tails → parametric VaR understates tail risk** — 2/2 unaided on closing check. Stated: "real equity returns have fat tails so parametric VaR understates tail risk — too low at 99%."
- **Three methods agree but for different reasons** — noticed that parametric was slightly more negative than historical (opposite of the fat-tail prediction), and accepted the explanation: historical 99% rests on only ~5 data points (unstable), so the comparison is noisy.

---

## Scaffolding log

| What they believed | What was wrong | What was said | Landed? |
|---|---|---|---|
| CSVLoader class is appropriate | No state to encapsulate between calls | "What state would it hold between calls?" → "nothing" → free function | Yes, unaided conclusion |
| Definitions belong in .h | Definitions in headers cause ODR issues if included in multiple TUs | "What's actually in this file?" twice — first pass didn't trigger fix; second did | Yes |
| load_prices ignores filepath param | Hardcoded "data/prices.csv" instead of using parameter | Pointed at the parameter vs body mismatch | Yes, immediate fix |
| std::sort works on const& | const means non-mutable; sort modifies in place | "Will sort compile on a const reference? what does const promise?" | Yes |
| Index into `log_returns` after making sorted copy | Sorting a copy then reading from the original is meaningless | "Which vector should you read from?" | Yes, immediate |
| (sigma/2)^2 in GBM drift | That's σ²/4, not σ²/2 | "Expand that algebraically — what does it equal?" | Yes, self-corrected |

---

## Retrieval scores

| Date | Topic | Score | Re-test |
|------|-------|-------|---------|
| 2026-05-22 | GBM formula — drift + shock notation (W6 spaced rep, overdue 2026-05-20) | 1/2 — wrote (σ/2)² not σ²/2; self-corrected after "expand algebraically" | 2026-05-23 |
| 2026-05-22 | VaR index arithmetic — 95% and 99% for 500 returns | 2/2 unaided — index 24 and index 4, both immediate | — |
| 2026-05-22 | Fat tails closing check — parametric VaR understates tail risk at 99% | 2/2 unaided — full answer without prompting | — |

---

## Momentum and blockers

- **Pace vs schedule:** On track. W8 Day 2 complete; implementation running.
- **Next-session risk:** Code review (Day 3) will likely surface: (1) no guard if `load_prices` returns an empty vector before calling `log_returns`; (2) no check that the file opened successfully; (3) output formatting could be cleaner with `std::setprecision`.
- **One thing to re-test first:** GBM formula notation — carry-forward from today (1/2).

---

## Next session's contract

- **First action:** W8 Day 3 — code review of `projects/week08/var_model.cpp` as Code Reviewer: check naming, missing guards (empty returns from load_prices, file-open failure in csv_loader), and output formatting.
- **Retrieval question:** "Write the GBM path formula from memory — both terms with correct notation."
- **Carry-forwards:** GBM formula notation (wrote (σ/2)² instead of σ²/2; 1/2 today).
