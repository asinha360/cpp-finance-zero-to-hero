# W5 Day 4 — Code Review: RNG Harness + Histogram

**Date:** 2026-05-10
**Session type:** Day 4 — Code review + fix blocking issues

---

## Retrieval practice

**Topic:** BS inputs, symbols, directional effects on a European call — due 2026-05-10 (from W4 Day 1)

| Input | Symbol | Direction | Reason |
|-------|--------|-----------|--------|
| Current price | S | ↑ raises | Higher stock → more valuable right to buy |
| Strike | K | ↑ lowers | Higher threshold to be profitable |
| Risk-free rate | r | ↑ raises | Lowers PV of K payment → cheaper to acquire stock via option |
| Volatility | σ | ↑ raises | Asymmetric payoff: downside floored at 0, upside unlimited |
| Time to expiry | T | ↑ raises | More time → more chance of ending in the money |

**Score: 5/5 directions correct, unaided.** [FIRST TIME] σ asymmetry reasoning landed without prompting — prior session (W5 Day 2) needed coaching on the downside-cap argument.

---

## Code review findings

**File reviewed:** [projects/week05/rng_harness.cpp](../projects/week05/rng_harness.cpp)

### BLOCKING — resolved

- `mean` and `sample_variance` returned sentinel `0.0` on bad input — same anti-pattern as W4's `return 1`. Replaced with `assert` in both functions. Crash loudly; don't return plausible-looking garbage.

### SUGGESTIONS — resolved

- `(int)` cast in for loops → range-for with `const double& x` (same fix as W3; applied correctly after one type-error correction: `const int&` → `const double&`).
- `setprecision(2)` → `setprecision(5)` for diagnostic output.
- `assert(size() > 1)` → `assert(size() >= 2)` for clarity and to avoid signed/unsigned comparison.
- `/ random_vector.size()` → `/ static_cast<double>(random_vector.size())` in both `mean` and `sample_variance`.

### INCOMPLETE → completed

- Histogram: not present at review start. Built from scratch during session.

---

## Histogram — built from scratch

**Algorithm understood and implemented:**
1. Bins: `lower = -4.0`, `bin_width = 0.5`, `num_bins = 16`
2. `std::vector<int> counts(num_bins, 0)` — pre-initialized with 16 zeros
3. Counting loop: `bin_index = static_cast<int>((x - lower) / bin_width)`, clamp to [0, 15], increment `counts[bin_index]++`
4. Print loop: index-based (range-for not applicable — need `i` to compute left edge), `setw(5)` for alignment, `counts[i] / 10000` stars

**Errors made and corrected:**
- `const int& x` in range-for (same type error as before — fixed after one prompt)
- `push_back(bin_index)` instead of `counts[bin_index]++` — conceptual: pre-initialized slots must be incremented, not appended to
- `num_stars * "*"` — Python syntax; C++ needs an inner star loop
- `i < num_bins - 1` off-by-one (missed last bin)
- `int left_edge` instead of `double` (labels truncated to integers)

**Output confirmed:** bell-curve shape visible, symmetric, tails near ±4 sparse.

---

## Key conceptual wins

- **Range-for vs index loop distinction:** range-for requires an existing collection to iterate over. `rand_vector`'s loop repeats N times without iterating a collection — index loop is correct there. Retrieved unaided.
- **σ asymmetry:** downside of a call is floored at 0 (you don't exercise); upside is unbounded. Higher vol lifts both tails but only the good one benefits you. Stated cleanly without prompting. [FIRST TIME]
- **`push_back` vs indexed increment:** `push_back` grows the vector; pre-initialized slots need `counts[i]++`. Conceptual distinction clear after one explanation.
- **`static_cast<int>(floor(...))` pattern:** explicit narrowing conversion; intent is visible to the reader.

---

## Re-test schedule

| Topic | Re-test on |
|-------|-----------|
| BS inputs + directions (5/5) | 2026-05-17 |
| σ asymmetry — downside floor argument | 2026-05-17 |
| Range-for vs index loop — when each applies | 2026-05-17 |
| `push_back` vs pre-initialized increment | 2026-05-14 |

---

## Next session

**W5 Day 5 (1h):** Acceptance-test run, retrospective, update week ledger.
- Compile clean under `-Wall -Wextra -std=c++20` ✓ (confirmed this session)
- Both asserts pass ✓ (mean 0.00179, stdev 0.99992)
- Histogram output confirmed ✓
- Retrospective to write at `projects/week05/retrospective.md`

---

## Full session record — 2026-05-10 (appended by closing sequence)

### 4a. Session header

- **Date:** 2026-05-10
- **Curriculum week/day:** Week 5, Day 4
- **Roles active:** Learning Coach (opening retrieval) → Code Reviewer (harness review) → Learning Coach (histogram coaching)
- **Estimated time:** ~2 hours
- **Attempted:** Spaced retrieval on BS inputs; code review of `rng_harness.cpp`; histogram implementation from scratch.
- **Completed:** All items — retrieval 5/5, all code review issues resolved, histogram output confirmed.

---

### 4b. Exercises and concepts

#### Exercise 1 — Spaced retest: BS inputs + directional effects

- **Identifier:** Spaced retest — W4 Day 1 BS inputs (due 2026-05-10)
- **Learning objective:** Recall all five Black-Scholes inputs (name, symbol, directional effect on a European call) without notes; understand *why* each direction holds, not just the label.
- **Approach:** Closed-book, written answer before any feedback.
- **First attempt:** All five directions correct. Reasons given: S ↑ → call ↑; K ↑ → call ↓; r ↑ → call ↑; σ ↑ → call ↑; T ↑ → call ↑. No symbols provided unprompted.
- **Hints given:**
  - Rung 1 (~5 min): "You gave directions without reasons. Tell me *why* σ ↑ raises the call price." — Why this direction: directions alone are surface recall; the reasoning is the transferable knowledge.
  - Rung 1 (~8 min): "A call's payoff is max(S_T − K, 0). If the stock tanks hard, what is your loss?" — Why this direction: pointing at the floor forces the user to derive the asymmetry argument rather than recite it.
- **Result:** User stated "downside is the strike [corrected to: zero — call simply expires worthless], upside is potentially infinite." The asymmetry argument was then cleanly articulated: capped downside at 0, uncapped upside → higher vol lifts the beneficial tail more than it damages the non-existent harmful one. This is the correct mechanism.
- **Acceptance criteria:** N/A (retrieval exercise, not code). Score: 5/5 directions unaided. σ reasoning [FIRST TIME] stated without scaffolding — W5 Day 2 needed coaching on downside cap.
- **Complexity note:** N/A.

---

#### Exercise 2 — Code review: rng_harness.cpp

- **Identifier:** W5/rng_harness.cpp — code review
- **Learning objective:** Identify the silent-sentinel anti-pattern; apply range-for with correct element type; understand explicit casting; distinguish `push_back` from pre-initialized slot increment.
- **Approach:** Claude-as-Code-Reviewer read the file and listed findings; user responded with fixes, each applied independently.

**Blocking issue — `return 0.0` sentinel:**
- User correctly identified the fix ("replace with assert") after one prompt connecting it to W4's `return 1` anti-pattern.
- Fix applied: `assert(!random_vector.empty() && "Mean undefined for fewer than 1 return")` in `mean`; `assert(random_vector.size() >= 2 && "Variance undefined for fewer than 2 returns")` in `sample_variance`.
- Note: `assert(size() >= 2)` chosen over `> 1` for clarity (says "need at least 2") and to avoid comparing `size_t` (unsigned) with signed literal `1`.

**Suggestion — `(int)` cast in for loops:**
- User initially said "remove the casts?" without knowing the replacement. Coached to recall range-for from W3.
- First range-for attempt used `const int& x` — wrong element type. Corrected after one prompt (what type are the elements?).
- Correct form: `for (const double& x : random_vector)`. Applied in both `mean` and `sample_variance`.

**Suggestion — `setprecision(2)` → `setprecision(5)`:** Applied correctly without further coaching.

**Suggestion — `static_cast<double>` for division:** Applied correctly: `sum / static_cast<double>(random_vector.size())` in `mean`; `static_cast<double>(random_vector.size() - 1)` in `sample_variance`.

- **Compile result:** Clean under `-Wall -Wextra -std=c++20`. No warnings.
- **Runtime result:** Sample mean 0.00179 ∈ [-0.005, 0.005] ✓; stdev 0.99992 ∈ [0.995, 1.005] ✓.
- **Complexity note:** N/A.

---

#### Exercise 3 — Histogram implementation from scratch

- **Identifier:** W5/rng_harness.cpp — text histogram of N(0,1) samples
- **Learning objective:** Understand binning (dividing a range into equal-width buckets and counting samples per bucket); implement pre-initialized vector increment; use index loop where range-for is not applicable; understand scaling for text display.
- **Approach:** Algorithm explained by Claude; user implemented each phase (counting loop, then print loop) with errors corrected at each step.

**Algorithm explained:**
- Bins: `lower = -4.0`, `bin_width = 0.5`, `num_bins = 16`
- `std::vector<int> counts(num_bins, 0)` — pre-initialize all slots to zero
- For each sample: `bin_index = floor((x - lower) / bin_width)`, clamp to [0, 15], `counts[bin_index]++`
- Print: index loop (range-for loses `i` needed for left-edge computation), `setw(5)` for alignment, inner star loop

**Hints given — counting loop:**
- Rung 1 (~35 min): "What type are the elements of `local_random_vector`?" — Why this direction: `const int& x` is the same type error they corrected in `mean`; pointing at the type forces self-correction.
- Rung 1 (~38 min): "`push_back` *adds a new element to the end*. You already initialized 16 slots. What should you do instead of push_back?" — Why this direction: the conceptual error is confusing growth with mutation; naming what push_back does makes the error visible.

**Hints given — print loop:**
- Rung 3 (~55 min): Explained directly that `x` in `for (const int& x : counts)` is a *count* (~200,000), not a sample. Applying `(x - lower) / bin_width` to a count is meaningless. Solution: index loop so `i` is available for left-edge computation. — Why this direction: the user had confused the variable's domain; explaining the domain mismatch directly was necessary since the error was conceptual, not syntactic.
- Rung 1 (~70 min): "What does `num_stars * "*"` do in C++?" — implied that it doesn't work; user needed to recognize this is Python-style multiplication. Why this direction: naming the language boundary surfaces the fix without giving it.
- Rung 1 (~75 min): After seeing `i < num_bins - 1`: "You have 16 bins (0–15)." — off-by-one self-corrected after one prompt.
- Rung 1 (~77 min): "You're computing `lower + i * bin_width`. What type is the result?" — `int left_edge` was truncating. Self-corrected to `double`.

**Errors made — full list:**
1. `const int& x` in counting loop — same type error as mean (fixed after one prompt)
2. `double num_bins = 16` — count as floating point (fixed in polish pass: changed to `int`)
3. `push_back(bin_index)` instead of `counts[bin_index]++` — conceptual (fixed after explanation)
4. `num_stars * "*"` — Python syntax (fixed after one prompt; user wrote inner star loop)
5. `i < num_bins - 1` off-by-one (fixed after one prompt)
6. `int left_edge` truncating decimal labels (fixed after one prompt)
7. In print loop: reapplied counting formula to `x` from `counts` — domain confusion (fixed after rung-3 explanation)

**Output confirmed:** Bell-curve shape visible. Symmetric. Tallest bars near 0, sparse tails near ±4. Scaling set to `/ 10000` after `/ 5000` caused line-wrap.

**Compile:** Clean. **Runtime:** Correct shape.
- **Complexity note:** N/A this session. (One-pass O(N) over 1M samples for counting; O(num_bins) for printing — not discussed explicitly.)

---

### 4c. Conceptual wins

- **σ asymmetry — downside floor argument [FIRST TIME unaided]:** "Downside is capped at zero (call expires worthless), upside is potentially infinite. Higher vol lifts both tails but only the good one benefits you." No scaffolding needed; prior session needed the downside-cap question.
- **Range-for vs index loop — when each applies [unaided]:** "We are not iterating over a vector or STL containers that have multiple elements stored within — rather we're creating one with a set number of elements." Correct: range-for requires an existing collection; repeat-N loop needs only a counter.
- **`assert` with string literal [unaided]:** `assert(condition && "message")` pattern applied correctly — string literal is always truthy, so the && doesn't change behavior but the message appears in the crash output.
- **`static_cast<double>` for explicit narrowing [unaided after explanation]:** Applied correctly in two places.
- **`setw(5)` for alignment:** Understood and applied correctly on first attempt after concept explained.

---

### 4d. Scaffolding log

1. **Print-loop domain confusion.** User applied `(x - lower) / bin_width` to `x` from `counts`, where `x` is a count (~200,000), not a sample value. Result: nonsense bin index (~399,000), way out of bounds. What was wrong: the variable's domain changed between the two loops but the formula was copy-pasted. Correction: explained directly that `x` in a range-for over `counts` is a count, not a position — the index loop gives you `i` so you can compute `lower + i * bin_width`. Landed: yes, user rewrote the loop correctly on next attempt.

2. **`num_stars * "*"` — Python syntax.** User wrote string multiplication as in Python. What was wrong: C++ has no operator to repeat a string N times. Correction: told user an inner loop is needed — `for (int j = 0; j < num_stars; j++) std::cout << "*"`. Landed: yes, applied correctly.

3. **σ reasoning — minor.** User initially said "the strike is the greatest loss on the call." What was wrong: in terms of payoff, the floor is 0 (the option expires worthless), not −K. The premium paid is the real loss in practice but that is a P&L concept, not a payoff concept. Correction: "The floor is zero — you simply don't exercise." Landed: yes, user revised the argument correctly.

---

### 4e. Retrieval scores

| Prompt | Score | Notes |
|--------|-------|-------|
| Opening: BS inputs — all 5, symbols, directions (due 2026-05-10) | 2/2 | All 5 correct, unaided. |
| Probe: σ — *why* does higher vol raise the call price? | 2/2 | Asymmetry argument stated after one directional probe; [FIRST TIME] without full scaffolding. |
| Closing: BS inputs — all 5, symbols, directions + σ reason | 1/2 | Directions 5/5 unaided; σ one-sentence reason omitted despite being explicitly asked for. Concepts are there — dropped under closing pressure. Re-test σ reasoning at start of Day 5. |

---

### 4f. Momentum and blockers

Pace is on track. W5 has four of five days complete; only the retrospective and formal acceptance-test run remain. The histogram implementation showed a recurring pattern: the user applies the correct loop type from one context but reverts to the wrong type or wrong operation in a new context (range-for element type, push_back vs increment). This is normal at this stage — the pattern needs one more repetition before it transfers fully. The most likely blocker next session is not technical: the retrospective requires honest reflection on what slipped vs what landed, which takes effort. The one concept worth re-testing at the very start of Day 5 is the `push_back` vs pre-initialized increment distinction — it scored 0 this session (corrected after direct explanation) and is likely to recur in W6 when class member vectors are introduced.

---

### 4g. Next session contract

- **First action:** Compile `projects/week05/rng_harness.cpp` under `g++ -Wall -Wextra -std=c++20 rng_harness.cpp -o rng && ./rng`, confirm both asserts pass and histogram prints; then open `templates/retrospective-template.md` and write `projects/week05/retrospective.md`.
- **Retrieval question to open with:** "You have `std::vector<int> counts(16, 0)`. You loop over 1M samples and want to increment the right bin. Why is `counts.push_back(bin_index)` wrong, and what does `counts[bin_index]++` do instead?" Also: "Higher σ raises a call's price — explain why in one sentence."
- **Carry-forwards:** σ asymmetry reason dropped at closing check (1/2) — re-test first thing. `push_back` vs pre-initialized increment scored 0 — re-test before W6.
